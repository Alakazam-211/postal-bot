[package]
name = "p5-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Postal core types: addresses, peer types, homes, roster, mailbox"
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
ulid = { version = "1", features = ["rand"] }

[dev-dependencies]
tempfile = "3"
